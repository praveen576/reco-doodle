<%= form_with(model: roleuser, local: true) do |form| %>
  <% if roleuser.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(roleuser.errors.count, "error") %> prohibited this roleuser from being saved:</h2>

      <ul>
      <% roleuser.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
